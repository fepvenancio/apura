---
phase: 07-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/csv.ts
  - frontend/src/lib/csv.test.ts
  - frontend/src/components/query/result-panel.tsx
  - frontend/src/app/(dashboard)/reports/[id]/page.tsx
  - frontend/src/app/(dashboard)/reports/[id]/print/page.tsx
  - frontend/src/app/(dashboard)/reports/[id]/print/print.css
autonomous: true
requirements:
  - EXPORT-01
  - EXPORT-02

must_haves:
  truths:
    - "User can click Export CSV on query results and receives a .csv file download"
    - "User can click Export CSV on a report and receives a .csv file download"
    - "CSV output correctly escapes values containing commas, quotes, and newlines"
    - "User can click Print on a report and see a print-optimized view"
    - "Print view renders report name, SQL, explanation, and data table cleanly on paper"
  artifacts:
    - path: "frontend/src/lib/csv.ts"
      provides: "Shared CSV generation utility"
      exports: ["generateCsv"]
    - path: "frontend/src/app/(dashboard)/reports/[id]/print/page.tsx"
      provides: "Print-optimized report view page"
      min_lines: 40
  key_links:
    - from: "frontend/src/components/query/result-panel.tsx"
      to: "frontend/src/lib/csv.ts"
      via: "import { generateCsv }"
      pattern: "import.*generateCsv.*csv"
    - from: "frontend/src/app/(dashboard)/reports/[id]/page.tsx"
      to: "frontend/src/lib/csv.ts"
      via: "import { generateCsv }"
      pattern: "import.*generateCsv.*csv"
    - from: "frontend/src/app/(dashboard)/reports/[id]/page.tsx"
      to: "frontend/src/app/(dashboard)/reports/[id]/print/page.tsx"
      via: "Print button links to /reports/[id]/print"
      pattern: "print"
---

<objective>
Add CSV export utility and print-optimized report view to complete Phase 7 export features.

Purpose: Users need to get data out of Apura -- CSV for spreadsheet work, print-to-PDF for management reports.
Output: Shared CSV utility used by query results and reports, plus a /reports/[id]/print page with @media print CSS.
</objective>

<execution_context>
@/Users/address0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/address0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/lib/types.ts
@frontend/src/components/query/result-panel.tsx
@frontend/src/app/(dashboard)/reports/[id]/page.tsx
@frontend/src/stores/query-store.ts

<interfaces>
<!-- Key types the executor needs -->

From frontend/src/lib/types.ts:
```typescript
export interface QueryColumn {
  name: string;
  type: string;
}

export interface QueryResult {
  id: string;
  naturalLanguage: string;
  sql: string;
  explanation: string;
  columns: QueryColumn[];
  rows: Record<string, unknown>[];
  rowCount: number;
  executionTimeMs: number;
  createdAt: string;
}

export interface Report {
  id: string;
  name: string;
  description?: string;
  queryId: string;
  chartConfig?: Record<string, unknown>;
  layoutConfig?: Record<string, unknown>;
  lastRunAt?: string;
  createdAt: string;
  updatedAt: string;
}
```

From frontend/src/components/ui/button.tsx:
```typescript
type ButtonVariant = "primary" | "secondary" | "ghost" | "danger";
type ButtonSize = "sm" | "md" | "lg";
// Usage: <Button variant="secondary" size="sm" onClick={fn}>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared CSV utility and fix both export call sites</name>
  <files>frontend/src/lib/csv.ts, frontend/src/lib/csv.test.ts, frontend/src/components/query/result-panel.tsx, frontend/src/app/(dashboard)/reports/[id]/page.tsx</files>
  <read_first>
    - frontend/src/components/query/result-panel.tsx (existing inline CSV logic to extract)
    - frontend/src/app/(dashboard)/reports/[id]/page.tsx (second inline CSV logic with quoting bug)
    - frontend/src/lib/types.ts (QueryColumn, QueryResult types)
  </read_first>
  <behavior>
    - generateCsv([{name:"A",type:"text"},{name:"B",type:"number"}], [{A:"hello",B:1},{A:"world",B:2}]) returns "A,B\nhello,1\nworld,2"
    - Values containing commas are quoted: "has,comma" becomes '"has,comma"'
    - Values containing double quotes are escaped: 'say "hi"' becomes '"say ""hi"""'
    - Values containing newlines are quoted: "line1\nline2" becomes '"line1\nline2"'
    - Null/undefined values become empty string
    - downloadCsv() triggers a Blob download with text/csv MIME type and UTF-8 BOM for Excel compatibility
  </behavior>
  <action>
    1. Create `frontend/src/lib/csv.ts` with two exported functions:
       - `generateCsv(columns: QueryColumn[], rows: Record<string, unknown>[]): string` -- builds CSV string with proper escaping. Header row from column names. Each data row: map columns, convert null/undefined to empty string, escape values containing commas/quotes/newlines by wrapping in double quotes and doubling any internal quotes. Join with commas, join rows with \n.
       - `downloadCsv(columns: QueryColumn[], rows: Record<string, unknown>[], filename: string): void` -- calls generateCsv, prepends UTF-8 BOM (\uFEFF) for Excel compatibility, creates Blob with type "text/csv;charset=utf-8;", creates object URL, triggers download via anchor click, revokes URL.
    2. Create `frontend/src/lib/csv.test.ts` with tests for generateCsv covering: basic output, comma escaping, quote escaping, newline escaping, null handling, empty rows.
    3. Update `frontend/src/components/query/result-panel.tsx`: remove inline handleExportCSV logic, import `downloadCsv` from `@/lib/csv`, replace onClick with `downloadCsv(result.columns, result.rows, \`apura-resultado-\${new Date().toISOString().slice(0,10)}.csv\`)`.
    4. Update `frontend/src/app/(dashboard)/reports/[id]/page.tsx`: remove inline handleExportCsv logic, import `downloadCsv` from `@/lib/csv`, replace onClick with `downloadCsv(result.columns, result.rows, \`\${report?.name || "report"}.csv\`)`.

    The report detail page version has a bug: it does not escape values containing commas or newlines (only handles quotes). The shared utility fixes this.
  </action>
  <verify>
    <automated>cd /Users/address0/Documents/Repos/apura/frontend && npx vitest run src/lib/csv.test.ts 2>/dev/null || npx jest --passWithNoTests src/lib/csv.test.ts 2>/dev/null || echo "Run test manually"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function generateCsv" frontend/src/lib/csv.ts
    - grep -q "export function downloadCsv" frontend/src/lib/csv.ts
    - grep -q "import.*downloadCsv.*csv" frontend/src/components/query/result-panel.tsx
    - grep -q "import.*downloadCsv.*csv" frontend/src/app/\(dashboard\)/reports/\[id\]/page.tsx
    - ! grep -q "handleExportCSV\|handleExportCsv" frontend/src/components/query/result-panel.tsx
    - ! grep -q "handleExportCsv" frontend/src/app/\(dashboard\)/reports/\[id\]/page.tsx
  </acceptance_criteria>
  <done>Both query results page and report detail page use the shared downloadCsv utility. Inline CSV generation removed. CSV output correctly escapes commas, quotes, and newlines.</done>
</task>

<task type="auto">
  <name>Task 2: Create print-optimized report view page</name>
  <files>frontend/src/app/(dashboard)/reports/[id]/print/page.tsx, frontend/src/app/(dashboard)/reports/[id]/print/print.css, frontend/src/app/(dashboard)/reports/[id]/page.tsx</files>
  <read_first>
    - frontend/src/app/(dashboard)/reports/[id]/page.tsx (report detail page -- add Print button here)
    - frontend/src/lib/api.ts (api.getReport and api.runReport methods)
    - frontend/src/lib/types.ts (Report, QueryResult types)
    - frontend/src/components/ui/button.tsx (Button component API)
  </read_first>
  <action>
    1. Create `frontend/src/app/(dashboard)/reports/[id]/print/print.css` with @media print styles:
       - Hide sidebar, topbar, buttons, navigation (use `.no-print { display: none }` class)
       - Set body background to white, text to black
       - Table: full width, 1px solid #333 borders, 4px 8px cell padding, left-aligned text
       - Table header: bold, background #f0f0f0
       - Numeric cells: text-align right, tabular-nums
       - Page margins: @page { margin: 1.5cm }
       - Avoid page breaks inside table rows: break-inside: avoid
       - Report title: font-size 18pt, margin-bottom 8pt
       - SQL block: font-family monospace, font-size 8pt, background #f5f5f5, padding 8px, border 1px solid #ddd
       - Explanation: font-size 10pt, color #333
       - Footer: page date stamp, font-size 8pt, color #666

    2. Create `frontend/src/app/(dashboard)/reports/[id]/print/page.tsx` as a "use client" page:
       - Fetch report via `api.getReport(id)` and run it via `api.runReport(id)` on mount
       - Import `./print.css`
       - Render a clean, print-friendly layout (NO dashboard layout -- just the content):
         - Report name as h1
         - Report description (if any) as subtitle
         - "Generated on: {date}" line
         - SQL query in a pre/code block
         - Explanation paragraph
         - Data table: plain HTML table with thead/tbody, column headers, all rows (no pagination -- print needs all data)
         - Format numbers using the same formatNumber/formatCurrency utils from @/lib/utils
       - Include a "Print" button (class="no-print") that calls window.print()
       - Include a "Back" link (class="no-print") using router.back()
       - On screen (not print): show with light background, max-width 900px, centered, readable

    3. Update `frontend/src/app/(dashboard)/reports/[id]/page.tsx`:
       - Add a "Print" button (Printer icon from lucide-react) next to the existing "Export CSV" button in the actions section
       - Import `Printer` from lucide-react
       - OnClick: `window.open(\`/reports/\${id}/print\`, '_blank')` -- opens print view in new tab
       - Button should use variant="secondary" to match the Export CSV button style
  </action>
  <verify>
    <automated>cd /Users/address0/Documents/Repos/apura && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - test -f frontend/src/app/\(dashboard\)/reports/\[id\]/print/page.tsx
    - test -f frontend/src/app/\(dashboard\)/reports/\[id\]/print/print.css
    - grep -q "@media print" frontend/src/app/\(dashboard\)/reports/\[id\]/print/print.css
    - grep -q "window.print" frontend/src/app/\(dashboard\)/reports/\[id\]/print/page.tsx
    - grep -q "Printer" frontend/src/app/\(dashboard\)/reports/\[id\]/page.tsx
    - grep -q "no-print" frontend/src/app/\(dashboard\)/reports/\[id\]/print/print.css
  </acceptance_criteria>
  <done>Print-optimized report view exists at /reports/[id]/print with @media print CSS. Report detail page has a Print button that opens it in a new tab. User can browser print-to-PDF from the print view.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `cd frontend && npx tsc --noEmit`
2. CSV tests pass
3. Grep confirms no inline CSV generation remains in result-panel.tsx or reports/[id]/page.tsx
4. Print page exists and has @media print CSS
5. Print button exists on report detail page
</verification>

<success_criteria>
- EXPORT-01: CSV download works from both query results and report detail page using shared utility
- EXPORT-02: Print-optimized view accessible from report detail page, renders all data without pagination, produces clean output via browser print
</success_criteria>

<output>
After completion, create `.planning/phases/07-export/07-01-SUMMARY.md`
</output>
