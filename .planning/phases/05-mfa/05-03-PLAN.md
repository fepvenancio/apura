---
phase: 05-mfa
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/api-gateway/src/routes/auth.ts
  - packages/api-gateway/src/routes/org.ts
  - packages/api-gateway/src/routes/__tests__/mfa.test.ts
autonomous: true
requirements:
  - MFA-04

must_haves:
  truths:
    - "When org has mfa_required=1 and user has mfa_enabled=0, login returns mfaSetupRequired:true after password verification"
    - "GET /org/members returns mfa_enabled field for each team member"
    - "PATCH /org/settings accepts mfa_required field for owner/admin roles"
  artifacts:
    - path: "packages/api-gateway/src/routes/auth.ts"
      provides: "Org MFA enforcement check in login flow"
      contains: "mfaSetupRequired"
    - path: "packages/api-gateway/src/routes/org.ts"
      provides: "mfa_enabled in member listing, mfa_required in org settings"
      contains: "mfa_enabled"
  key_links:
    - from: "packages/api-gateway/src/routes/auth.ts"
      to: "organizations.mfa_required"
      via: "D1 query in login handler"
      pattern: "mfa_required"
---

<objective>
Implement org-level MFA enforcement: when an org requires MFA, users without it are directed to set up MFA during login. Also expose mfa_enabled in member listings and mfa_required in org settings.

Purpose: Allows org admins to mandate MFA for all members, with graceful onboarding (redirect to setup, not lockout).
Output: Modified login flow for org enforcement, updated org routes.
</objective>

<execution_context>
@/Users/address0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/address0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mfa/05-RESEARCH.md
@.planning/phases/05-mfa/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 modifications -->

From packages/api-gateway/src/routes/auth.ts (after Plan 01):
```typescript
// Login handler now checks user.mfa_enabled
// If mfa_enabled=1: returns { mfaRequired: true, mfaToken }
// If mfa_enabled=0: returns normal tokens
// PLAN 03 adds: check org.mfa_required when user.mfa_enabled=0
```

From packages/api-gateway/src/services/org-db.ts (after Plan 01):
```typescript
// ORG_COLUMNS now includes 'mfa_required'
// USER_COLUMNS now includes 'mfa_enabled'
```

From packages/api-gateway/src/routes/org.ts:
```typescript
// GET /org/members returns team member list
// PATCH /org/settings updates org settings
// Need to add mfa_enabled to member query SELECT
// Need to ensure mfa_required accepted in settings update
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Org MFA enforcement in login and member/settings API updates</name>
  <files>
    packages/api-gateway/src/routes/auth.ts,
    packages/api-gateway/src/routes/org.ts,
    packages/api-gateway/src/routes/__tests__/mfa.test.ts
  </files>
  <read_first>
    packages/api-gateway/src/routes/auth.ts,
    packages/api-gateway/src/routes/org.ts,
    packages/api-gateway/src/routes/__tests__/mfa.test.ts,
    packages/api-gateway/src/services/org-db.ts
  </read_first>
  <action>
    1. Modify `packages/api-gateway/src/routes/auth.ts` login handler:
       After password verification succeeds and BEFORE the existing mfa_enabled check (from Plan 01):
       - Query the user's organization: `SELECT mfa_required FROM organizations WHERE id = ?` using user.org_id.
       - Add a new branch: if `org.mfa_required === 1` AND `user.mfa_enabled === 0`:
         - Issue a temporary token (same as MFA challenge, but stored with a different prefix): `mfa_setup:{token}` in KV with `{ userId, orgId, role }`, TTL 600s (10 min for setup).
         - Return `{ success: true, data: { mfaSetupRequired: true, mfaToken: token } }`.
         - The frontend will use this token to redirect the user to the MFA setup page, where they complete setup and then re-login.
       - The existing `if (user.mfa_enabled)` branch remains (returns mfaRequired: true with challenge token).
       - Non-MFA, non-enforced orgs continue with normal token issuance.

    2. Update `packages/api-gateway/src/routes/org.ts`:
       - In the GET /org/members handler (or equivalent member listing query), add `mfa_enabled` to the SELECT columns for users. This allows the frontend to show MFA status per member.
       - Verify that PATCH /org/settings (or the equivalent org update handler) accepts `mfa_required` in the update payload. Since `ORG_COLUMNS` already includes `'mfa_required'` from Plan 01, this should work via the existing `updateOrg()` flow — just confirm no additional validation blocks it.

    3. Add tests to `packages/api-gateway/src/routes/__tests__/mfa.test.ts`:
       **Org enforcement tests (MFA-04):**
       - Test: login with org.mfa_required=1 and user.mfa_enabled=0 returns mfaSetupRequired:true.
       - Test: login with org.mfa_required=1 and user.mfa_enabled=1 returns mfaRequired:true (normal MFA flow).
       - Test: login with org.mfa_required=0 and user.mfa_enabled=0 returns normal tokens (no MFA).
       - Test: GET /org/members response includes mfa_enabled field for each member.
  </action>
  <verify>
    <automated>cd /Users/address0/Documents/Repos/apura/packages/api-gateway && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - auth.ts login handler checks org.mfa_required when user.mfa_enabled=0
    - auth.ts returns mfaSetupRequired:true when org requires MFA but user hasn't set it up
    - org.ts GET /org/members SELECT includes mfa_enabled column
    - org.ts PATCH /org/settings accepts mfa_required field
    - 4 new test cases for org enforcement pass
    - All existing tests still pass
  </acceptance_criteria>
  <done>Org-level MFA enforcement active: login redirects users to MFA setup when org requires it, member listing shows MFA status, org settings accepts mfa_required toggle</done>
</task>

<task type="auto">
  <name>Task 2: Frontend org enforcement handling</name>
  <files>
    frontend/src/app/(auth)/login/page.tsx,
    frontend/src/app/(auth)/login/mfa/page.tsx,
    frontend/src/stores/auth-store.ts,
    frontend/src/lib/api.ts
  </files>
  <read_first>
    frontend/src/app/(auth)/login/page.tsx,
    frontend/src/app/(auth)/login/mfa/page.tsx,
    frontend/src/stores/auth-store.ts,
    frontend/src/lib/api.ts
  </read_first>
  <action>
    1. Update `frontend/src/lib/api.ts`:
       - In the `login()` method, also check for `mfaSetupRequired: true` in the response.
       - Create `MfaSetupRequiredError` (extends Error) with `mfaToken: string` property.
       - Throw `MfaSetupRequiredError` when backend returns `mfaSetupRequired: true`.

    2. Update `frontend/src/stores/auth-store.ts`:
       - Add `mfaSetupRequired: boolean` state field (default false).
       - In login action catch: if error is `MfaSetupRequiredError`, set `mfaSetupRequired: true` and `pendingMfaToken: mfaToken`.

    3. Update `frontend/src/app/(auth)/login/page.tsx`:
       - In handleSubmit catch: also check for `MfaSetupRequiredError`.
       - If caught: redirect to `/settings/security` (or `/login/mfa-setup`) so user can set up MFA. Show a toast or message explaining "A sua organizacao requer autenticacao de dois fatores. Configure agora." (Your organization requires two-factor authentication. Set it up now.)
       - Since the user is not yet authenticated (no real tokens), redirect to a special unauthenticated MFA setup flow. Simplest approach: redirect to `/login/mfa` with a state indicator that shows the setup UI instead of verify UI.

    4. Update `frontend/src/app/(auth)/login/mfa/page.tsx`:
       - Check auth store: if `mfaSetupRequired` is true (not just `mfaRequired`):
         - Show a different flow: call `api.setupMfa()` will NOT work because user is unauthenticated.
         - Alternative approach (simpler, avoids unauthenticated setup complexity): Show message "A sua organizacao requer 2FA. Inicie sessao e configure nas definicoes de seguranca." This won't work either since they can't log in.
         - BEST approach: When org requires MFA and user hasn't set up, the backend should still issue temporary limited tokens (like the mfa_setup token). The frontend redirects to `/login/mfa` which detects `mfaSetupRequired` and shows the full setup flow (QR + confirm) using the temporary token for API auth. After setup completes, user must login again.
         - Implement: when `mfaSetupRequired` is true, page shows:
           a. Info banner: "A sua organizacao requer autenticacao de dois fatores."
           b. "Configurar 2FA" button that calls `/api/mfa/setup` using a special auth header with the mfa_setup token.
           c. Shows QR code + secret + confirm input (same as security settings page but extracted into a reusable component or duplicated).
           d. After confirm succeeds, show backup codes, then redirect to `/login` with success message "2FA configurado. Inicie sessao novamente."
         - NOTE: The `/api/mfa/setup` and `/api/mfa/confirm` routes require auth middleware. For org enforcement flow, the backend mfa_setup token must bypass auth OR these routes need an alternative auth path. Simplest: add the mfaToken as a Bearer token and have the MFA routes accept it from KV `mfa_setup:{token}` as an alternative to full JWT auth. Add this logic to the mfa.ts routes: if no valid JWT, check for `mfa_setup:*` token in KV.
         - Actually, the SIMPLEST approach that avoids backend changes: after login returns `mfaSetupRequired`, show a message page saying "A sua organizacao requer 2FA. Contacte o administrador ou configure 2FA na proxima vez que iniciar sessao." and provide a link to re-try login. This is NOT a great UX.
         - RECOMMENDED approach (minimal backend change): In auth.ts, when returning `mfaSetupRequired`, also issue a short-lived JWT (5 min) with a special claim `mfa_setup_only: true`. The auth middleware allows this JWT but the MFA setup/confirm routes check for it. After MFA confirm succeeds, the user's account has MFA enabled and they can login normally.
         - FOR NOW: Use the simple redirect approach. The login page shows an alert: "A sua organizacao requer 2FA. Sera redirecionado para a configuracao." After password verification, issue a temporary auth token (reuse the mfa_setup KV token approach from backend task 1). Pass mfaToken as query param to a new page `/login/mfa-setup` which calls setup/confirm with the temporary token as auth. Keep this simple — the MFA routes in Plan 01 already check for authenticated user via `c.get('userId')`. Extend them to also accept `mfa_setup:` KV tokens.
         - FINAL DECISION (keep it simple for v1): When org requires MFA but user hasn't set it up, the backend issues temporary limited-scope JWTs (same as normal access tokens but with 5-min expiry). The user is "logged in" temporarily with these tokens, gets redirected to `/settings/security` to set up MFA. After setup, they stay logged in normally. The temporary tokens work because the auth middleware validates them normally. The risk is low: the user IS authenticated (password verified), they just need to add MFA. Add a check in auth middleware or a wrapper: if org.mfa_required and user.mfa_enabled=0 and request path is NOT /api/mfa/*, return 403 with `MFA_SETUP_REQUIRED`. This restricts the temporary session to only MFA setup routes.
         - SIMPLEST FINAL APPROACH: On `mfaSetupRequired`, the login handler issues real tokens (user IS password-authenticated after all) but with short TTL (5 min). Frontend persists them, redirects to `/settings/security`. The security page handles setup. After setup completes and MFA is enabled, the user gets normal token refresh. Add a middleware check: if org.mfa_required=1 and user.mfa_enabled=0, only allow requests to /api/mfa/* routes, return 403 for everything else.

       IMPLEMENTATION (this is the approach to code):
       - In auth.ts login: when org.mfa_required=1 and user.mfa_enabled=0, issue real JWT tokens with SHORT TTL (300s = 5 min) instead of the normal 3600s. Return `{ success: true, data: { accessToken, refreshToken, expiresIn: 300, user, mfaSetupRequired: true } }`.
       - The frontend receives tokens + mfaSetupRequired flag. Persist tokens, set user in store, redirect to `/settings/security`.
       - In authMiddleware (or a new middleware on non-MFA routes): if the JWT has a short TTL claim or if org.mfa_required=1 and user.mfa_enabled=0, restrict to /api/mfa/* only. Return 403 `MFA_SETUP_REQUIRED` for other routes. This is optional for v1 — the 5-min TTL is sufficient protection.

       Code changes for this page:
       - If `mfaSetupRequired` in auth store is true after login, `router.push('/settings/security')` instead of `/home`.
       - The security page (from Plan 02 Task 2) already handles MFA setup.
  </action>
  <verify>
    <automated>cd /Users/address0/Documents/Repos/apura && grep -c "mfaSetupRequired\|MfaSetupRequired" frontend/src/lib/api.ts frontend/src/stores/auth-store.ts frontend/src/app/\(auth\)/login/page.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - api.ts detects mfaSetupRequired response and throws MfaSetupRequiredError
    - auth-store.ts has mfaSetupRequired state field
    - login/page.tsx redirects to /settings/security when mfaSetupRequired
    - auth.ts issues short-TTL tokens (300s) for org-enforced MFA setup
    - User flow: login -> password ok -> org requires MFA -> redirect to security settings -> setup MFA -> done
  </acceptance_criteria>
  <done>Org MFA enforcement UX complete: users without MFA in MFA-required orgs are redirected to setup after password login</done>
</task>

</tasks>

<verification>
- `grep 'mfaSetupRequired' packages/api-gateway/src/routes/auth.ts` — backend returns setup-required flag
- `grep 'mfa_required' packages/api-gateway/src/routes/org.ts` — org settings accepts MFA toggle
- `grep 'mfa_enabled' packages/api-gateway/src/routes/org.ts` — member listing includes MFA status
- `cd packages/api-gateway && npx vitest run --reporter=verbose` — all tests pass
</verification>

<success_criteria>
- Org admin can toggle MFA requirement via PATCH /org/settings
- Login for user in MFA-required org without MFA setup: issues short-lived tokens, redirects to security settings
- GET /org/members includes mfa_enabled per member
- Frontend handles both mfaRequired (existing MFA user) and mfaSetupRequired (org enforcement, no MFA yet)
</success_criteria>

<output>
After completion, create `.planning/phases/05-mfa/05-03-SUMMARY.md`
</output>
