---
phase: 08-sharing-and-scheduled-reports
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - frontend/src/lib/types.ts
  - frontend/src/lib/api.ts
  - frontend/src/app/(dashboard)/reports/page.tsx
  - frontend/src/app/(dashboard)/schedules/page.tsx
  - frontend/src/app/(dashboard)/schedules/new/page.tsx
autonomous: true
requirements: [SHARE-01, SHARE-02, SCHED-01, SCHED-03]

must_haves:
  truths:
    - "User can toggle is_shared on a report from the reports page"
    - "Reports page shows My Reports and Shared tabs"
    - "Shared tab shows reports from other org members where is_shared=true"
    - "User can create a new schedule with frequency presets"
    - "Schedules page lists all schedules with status and next run"
    - "User can view run history for a schedule"
    - "All new pages are mobile-responsive"
  artifacts:
    - path: "frontend/src/app/(dashboard)/reports/page.tsx"
      provides: "Reports page with sharing toggle and shared/mine tabs"
      contains: "isShared"
    - path: "frontend/src/app/(dashboard)/schedules/page.tsx"
      provides: "Schedule list with run history expansion"
      contains: "schedule"
    - path: "frontend/src/app/(dashboard)/schedules/new/page.tsx"
      provides: "Schedule creation form with frequency presets"
      contains: "cronExpression"
    - path: "frontend/src/lib/types.ts"
      provides: "Updated Report type with isShared, ScheduleRun type"
      contains: "ScheduleRun"
    - path: "frontend/src/lib/api.ts"
      provides: "API methods for schedule runs and report sharing"
      contains: "getScheduleRuns"
  key_links:
    - from: "frontend/src/app/(dashboard)/reports/page.tsx"
      to: "/api/reports"
      via: "api.updateReport with isShared"
      pattern: "isShared"
    - from: "frontend/src/app/(dashboard)/schedules/page.tsx"
      to: "/api/schedules/:id/runs"
      via: "api.getScheduleRuns"
      pattern: "getScheduleRuns"
    - from: "frontend/src/app/(dashboard)/schedules/new/page.tsx"
      to: "/api/schedules"
      via: "api.createSchedule"
      pattern: "createSchedule"
---

<objective>
Build the frontend UI for report sharing and schedule management.

Purpose: The backend pipeline is fixed (Plan 01). Users now need UI to toggle report sharing, view shared reports, create/manage schedules, and view run history. This completes the user-facing experience for Phase 8.

Output: Reports page with sharing toggle + shared tab. Schedules page with list, run history, and creation form.
</objective>

<execution_context>
@/Users/address0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/address0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-sharing-and-scheduled-reports/08-RESEARCH.md
@.planning/phases/08-sharing-and-scheduled-reports/08-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 backend -- API contracts the frontend will consume -->

PUT /api/reports/:id — accepts `{ isShared: boolean }` (Plan 01 fixed naming)
GET /api/reports — returns `{ success: true, data: { items: Report[] } }` where Report has `is_shared`, `user_id`
GET /api/schedules — returns `{ success: true, data: { items: Schedule[] } }`
POST /api/schedules — accepts `{ reportId, cronExpression, timezone, outputFormat, recipients }`
GET /api/schedules/:id/runs — returns `{ success: true, data: { items: ScheduleRun[] } }`
GET /api/schedules/:scheduleId/runs/:runId/download — streams file

From frontend/src/lib/types.ts (current -- needs updates):
```typescript
export interface Report {
  id: string; name: string; description?: string; queryId: string;
  chartConfig?: Record<string, unknown>; layoutConfig?: Record<string, unknown>;
  lastRunAt?: string; createdAt: string; updatedAt: string;
  // MISSING: isShared, userId -- need to add
}

export interface Schedule {
  id: string; reportId: string; reportName: string; cron: string;
  timezone: string; enabled: boolean; nextRunAt?: string; lastRunAt?: string;
  lastRunStatus?: 'success' | 'error'; createdAt: string; updatedAt: string;
  // Needs: outputFormat, recipients
}
// MISSING: ScheduleRun type -- need to add
```

From frontend/src/components/layout/sidebar.tsx:
- `/reports` link already exists with label "Relatorios"
- `/schedules` link already exists with label "Agendamentos"

From frontend/src/stores/auth-store.ts:
- `useAuthStore((s) => s.user?.id)` gives current user ID for filtering shared reports
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update frontend types and API client, add sharing toggle + shared tab to reports page</name>
  <read_first>
    - frontend/src/lib/types.ts (current Report and Schedule types)
    - frontend/src/lib/api.ts (existing report/schedule methods)
    - frontend/src/app/(dashboard)/reports/page.tsx (current reports page)
    - frontend/src/stores/auth-store.ts (user ID access pattern)
  </read_first>
  <files>
    frontend/src/lib/types.ts,
    frontend/src/lib/api.ts,
    frontend/src/app/(dashboard)/reports/page.tsx
  </files>
  <action>
    **1. Update types.ts:**
    - Add `isShared?: boolean` and `userId?: string` to the `Report` interface
    - Add `outputFormat?: string` and `recipients?: string[]` to the `Schedule` interface
    - Add new `ScheduleRun` interface:
      ```typescript
      export interface ScheduleRun {
        id: string;
        scheduleId: string;
        status: 'pending' | 'queued' | 'running' | 'completed' | 'failed';
        outputUrl?: string;
        errorMessage?: string;
        startedAt: string;
        completedAt?: string;
      }
      ```

    **2. Update api.ts:**
    - Add `getScheduleRuns(scheduleId: string): Promise<ScheduleRun[]>` method that calls `GET /schedules/${scheduleId}/runs`
    - Add `downloadScheduleRun(scheduleId: string, runId: string)` method that opens a new window/tab to the download URL or uses fetch + blob download
    - Verify `updateReport(id, data)` method exists (it does at line 435) -- ensure it accepts `{ isShared: boolean }`

    **3. Rewrite reports/page.tsx with sharing:**
    - Import `useAuthStore` to get current user ID
    - Add tab state: `const [tab, setTab] = useState<'mine' | 'shared'>('mine')`
    - Add two tab buttons at the top: "Meus Relatórios" (mine) and "Partilhados comigo" (shared)
    - Style tabs with underline active indicator using Tailwind
    - Filter reports:
      - `mine` tab: `reports.filter(r => r.userId === userId)`
      - `shared` tab: `reports.filter(r => r.isShared && r.userId !== userId)`
    - Add a share toggle button to each report card (only on "mine" tab):
      - Use `Share2` icon from lucide-react
      - On click: `api.updateReport(report.id, { isShared: !report.isShared })`
      - Update local state after successful toggle
      - Show filled/active state when shared
    - Keep existing card layout (grid, 3 cols lg / 1 col mobile)
    - Keep existing delete, run, edit buttons
    - Add a subtle badge "Partilhado" on shared reports in the mine tab
    - Mobile responsive: tabs should be full-width on small screens, stack cards in single column

    All UI text in Portuguese (matching existing app convention):
    - "Meus Relatórios" / "Partilhados comigo" for tabs
    - "Partilhado" badge
    - "Partilhar" / "Deixar de partilhar" for share toggle tooltip
  </action>
  <acceptance_criteria>
    - grep "isShared" frontend/src/lib/types.ts
    - grep "ScheduleRun" frontend/src/lib/types.ts
    - grep "getScheduleRuns" frontend/src/lib/api.ts
    - grep "isShared" frontend/src/app/(dashboard)/reports/page.tsx
    - grep "Partilhados" frontend/src/app/(dashboard)/reports/page.tsx
    - grep "useAuthStore" frontend/src/app/(dashboard)/reports/page.tsx
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/address0/Documents/Repos/apura && grep -c "ScheduleRun" frontend/src/lib/types.ts && grep -c "getScheduleRuns" frontend/src/lib/api.ts && grep -c "isShared" frontend/src/app/\(dashboard\)/reports/page.tsx && grep -c "Partilhados" frontend/src/app/\(dashboard\)/reports/page.tsx && npx -w frontend tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Reports page has two tabs (mine/shared), share toggle on report cards, ScheduleRun type added, API client has getScheduleRuns method. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Build schedules list page with run history and schedule creation form</name>
  <read_first>
    - frontend/src/lib/api.ts (schedule API methods after Task 1 updates)
    - frontend/src/lib/types.ts (Schedule, ScheduleRun types after Task 1)
    - frontend/src/app/(dashboard)/reports/page.tsx (card layout pattern)
    - frontend/src/components/ui/card.tsx (Card component API)
    - frontend/src/components/ui/input.tsx (Input component API)
    - frontend/src/components/ui/button.tsx (Button component API)
    - frontend/src/components/layout/topbar.tsx (Topbar pattern)
  </read_first>
  <files>
    frontend/src/app/(dashboard)/schedules/page.tsx,
    frontend/src/app/(dashboard)/schedules/new/page.tsx
  </files>
  <action>
    **1. Create schedules/page.tsx -- Schedule list with run history:**
    - `"use client"` directive
    - Fetch schedules on mount via `api.getSchedules()`
    - Display as card list (similar to reports page pattern)
    - Each schedule card shows:
      - Report name, frequency description (human-readable from cron), timezone
      - Status: active/paused (from `enabled` field) with colored badge
      - Next run date (formatted with `formatRelativeDate`)
      - Last run date and status (success/error badge)
      - Action buttons: Edit (link to edit page -- just disable for now), Delete, Trigger Now
    - "Trigger Now" button calls `api.triggerSchedule(id)` and shows a toast/inline message
    - Delete calls `api.deleteSchedule(id)` with confirmation
    - Expandable run history section per schedule:
      - Toggle button "Ver histórico" / "Ocultar histórico"
      - When expanded, fetch `api.getScheduleRuns(scheduleId)`
      - Show table/list of runs: status (colored badge), started_at, completed_at, download link (if output_url exists)
      - Download link calls `api.downloadScheduleRun(scheduleId, runId)` or opens download URL
    - Empty state: "Ainda não tem agendamentos. Crie o primeiro para receber relatórios por email."
    - Link/button to create new: "Novo Agendamento" linking to `/schedules/new`
    - Topbar title: "Agendamentos"
    - Mobile responsive: single column on mobile, buttons stack vertically

    Helper function to convert cron to human-readable (inline):
    ```typescript
    function cronToLabel(cron: string): string {
      const [min, hour, dom, , dow] = cron.trim().split(/\s+/);
      if (dom !== '*' && /^\d+$/.test(dom)) return `Mensal (dia ${dom}, ${hour}:${min.padStart(2,'0')})`;
      if (dow !== '*' && /^\d+$/.test(dow)) {
        const days = ['Dom','Seg','Ter','Qua','Qui','Sex','Sáb'];
        return `Semanal (${days[parseInt(dow)]}, ${hour}:${min.padStart(2,'0')})`;
      }
      if (/^\d+$/.test(hour)) return `Diário (${hour}:${min.padStart(2,'0')})`;
      return cron;
    }
    ```

    **2. Create schedules/new/page.tsx -- Schedule creation form:**
    - `"use client"` directive
    - Topbar title: "Novo Agendamento"
    - Form fields:
      - **Report selector:** Dropdown/select that fetches reports via `api.getReports()` and shows report names. Required.
      - **Frequency preset:** Radio buttons or select with three options:
        - "Diário" (daily) -- shows time picker (hour:minute)
        - "Semanal" (weekly) -- shows day-of-week select + time picker
        - "Mensal" (monthly) -- shows day-of-month input (1-28) + time picker
      - **Timezone:** Select with common timezones, default "Europe/Lisbon". Options: Europe/Lisbon, Europe/Madrid, America/Sao_Paulo, UTC.
      - **Output format:** Select between "CSV" and "HTML". Default "csv".
      - **Recipients:** Email input that allows adding multiple emails. Show as tag/chip list. Validate email format client-side.
    - On submit: construct cron expression from frequency preset:
      - Daily at HH:MM: `"MM HH * * *"`
      - Weekly on DAY at HH:MM: `"MM HH * * DOW"`
      - Monthly on DAY at HH:MM: `"MM HH DAY * *"`
    - Call `api.createSchedule({ reportId, cronExpression, timezone, outputFormat, recipients })`
    - On success: redirect to `/schedules`
    - On error: show error message inline
    - Cancel button links back to `/schedules`
    - Mobile responsive: form fields stack vertically, full-width inputs

    Use existing UI components: Card, CardContent, Button, Input from `@/components/ui/`.
    Use Topbar from `@/components/layout/topbar`.
    Use icons from lucide-react: Clock, Plus, Trash2, Play, Download, ChevronDown, ChevronUp.
  </action>
  <acceptance_criteria>
    - grep "getSchedules" frontend/src/app/(dashboard)/schedules/page.tsx
    - grep "getScheduleRuns" frontend/src/app/(dashboard)/schedules/page.tsx
    - grep "cronExpression" frontend/src/app/(dashboard)/schedules/new/page.tsx
    - grep "recipients" frontend/src/app/(dashboard)/schedules/new/page.tsx
    - grep "createSchedule" frontend/src/app/(dashboard)/schedules/new/page.tsx
    - grep "Agendamentos" frontend/src/app/(dashboard)/schedules/page.tsx
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/address0/Documents/Repos/apura && test -f frontend/src/app/\(dashboard\)/schedules/page.tsx && test -f frontend/src/app/\(dashboard\)/schedules/new/page.tsx && grep -c "getSchedules" frontend/src/app/\(dashboard\)/schedules/page.tsx && grep -c "cronExpression" frontend/src/app/\(dashboard\)/schedules/new/page.tsx && npx -w frontend tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Schedules list page shows all schedules with status, expandable run history, trigger/delete actions. Creation form has frequency presets, report selector, recipient emails, timezone. Both pages mobile-responsive. TypeScript compiles.</done>
</task>

</tasks>

<verification>
Frontend delivers:
1. Reports page with mine/shared tabs and sharing toggle (SHARE-01, SHARE-02)
2. Schedules list page with run history (SCHED-01, SCHED-03)
3. Schedule creation form with frequency presets (SCHED-01)
4. All pages mobile-responsive

Full phase verification:
- `npx -w frontend tsc --noEmit` passes
- Reports page loads at /reports with two tabs
- Schedules page loads at /schedules with schedule list
- New schedule form at /schedules/new with frequency presets
</verification>

<success_criteria>
- All grep checks in acceptance_criteria pass
- Frontend TypeScript compilation succeeds
- Reports page has sharing toggle and shared tab
- Schedules page lists schedules with expandable run history
- Schedule creation form converts presets to cron expressions
- All pages render correctly on mobile viewport
</success_criteria>

<output>
After completion, create `.planning/phases/08-sharing-and-scheduled-reports/08-02-SUMMARY.md`
</output>
