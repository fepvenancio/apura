---
phase: 01-bug-fixes-and-cicd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api-gateway/package.json
  - packages/api-gateway/vitest.config.ts
  - packages/api-gateway/src/middleware/quota.ts
  - packages/api-gateway/src/routes/queries.ts
  - packages/api-gateway/src/routes/auth.ts
  - packages/api-gateway/src/middleware/__tests__/quota.test.ts
  - packages/api-gateway/src/routes/__tests__/queries.test.ts
  - packages/api-gateway/src/routes/__tests__/auth.test.ts
  - packages/ws-gateway/src/index.ts
  - packages/ws-gateway/package.json
  - packages/ws-gateway/vitest.config.ts
  - packages/ws-gateway/src/__tests__/internal-secret.test.ts
autonomous: true
requirements:
  - BUG-01
  - BUG-02
  - BUG-03
  - BUG-04
  - BUG-05

must_haves:
  truths:
    - "Quota middleware returns 503 when database query throws an error (fails closed)"
    - "AI orchestrator error messages stored in DB contain no raw error bodies or stack traces"
    - "New KV session is stored before old session is deleted during token refresh"
    - "Internal secret header is compared using timing-safe comparison, not string equality"
    - "Malformed JSON in password reset token returns 401, not 500"
  artifacts:
    - path: "packages/api-gateway/vitest.config.ts"
      provides: "Vitest configuration for api-gateway worker tests"
    - path: "packages/api-gateway/src/middleware/__tests__/quota.test.ts"
      provides: "Tests proving BUG-01 fix"
    - path: "packages/api-gateway/src/routes/__tests__/auth.test.ts"
      provides: "Tests proving BUG-03 and BUG-05 fixes"
    - path: "packages/api-gateway/src/routes/__tests__/queries.test.ts"
      provides: "Tests proving BUG-02 fix"
    - path: "packages/ws-gateway/src/__tests__/internal-secret.test.ts"
      provides: "Tests proving BUG-04 fix"
  key_links:
    - from: "packages/api-gateway/src/middleware/quota.ts"
      to: "503 response"
      via: "catch block returns error instead of calling next()"
      pattern: "return c\\.json.*503"
    - from: "packages/ws-gateway/src/index.ts"
      to: "crypto.subtle.timingSafeEqual"
      via: "timing-safe comparison function"
      pattern: "timingSafeEqual|timingSafeCompare"
---

<objective>
Fix all 5 known security and reliability bugs across api-gateway and ws-gateway, with test infrastructure and tests to prove each fix.

Purpose: Eliminate security vulnerabilities (timing attack, fail-open quota, error leakage) and reliability issues (session race condition, JSON parse crash) before adding any new features.
Output: Fixed bug code with passing Vitest tests for each bug.
</objective>

<execution_context>
@/Users/address0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/address0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bug-fixes-and-cicd/01-RESEARCH.md
@.planning/phases/01-bug-fixes-and-cicd/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Vitest test infrastructure for api-gateway and ws-gateway</name>
  <files>
    packages/api-gateway/package.json
    packages/api-gateway/vitest.config.ts
    packages/ws-gateway/package.json
    packages/ws-gateway/vitest.config.ts
  </files>
  <read_first>
    packages/api-gateway/package.json
    packages/ws-gateway/package.json
    packages/ai-orchestrator/package.json (reference for existing vitest setup pattern)
    packages/api-gateway/src/middleware/quota.ts (understand Hono context shape for mocking)
    packages/api-gateway/src/routes/auth.ts (understand env bindings needed in mocks)
    packages/ws-gateway/src/types.ts (understand Env type for ws-gateway)
  </read_first>
  <action>
1. Add vitest as devDependency to `packages/api-gateway/package.json`:
   - Add `"vitest": "^3.2.4"` to devDependencies
   - Add `"test": "vitest run"` to scripts

2. Create `packages/api-gateway/vitest.config.ts`:
   ```typescript
   import { defineConfig } from 'vitest/config';
   export default defineConfig({
     test: {
       environment: 'node',
       globals: true,
     },
   });
   ```

3. Add vitest as devDependency to `packages/ws-gateway/package.json`:
   - Add `"vitest": "^3.2.4"` to devDependencies
   - Add `"test": "vitest run"` to scripts

4. Create `packages/ws-gateway/vitest.config.ts` with same config pattern.

5. Run `npm install` at repo root to install vitest in both workspaces.

6. Verify vitest runs (even with 0 tests) in both packages:
   `cd packages/api-gateway && npx vitest run`
   `cd packages/ws-gateway && npx vitest run`
  </action>
  <verify>
    <automated>cd /Users/address0/Documents/Repos/apura && cd packages/api-gateway && npx vitest run 2>&1 | tail -5 && cd ../ws-gateway && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `packages/api-gateway/package.json` contains `"vitest": "^3.2.4"` in devDependencies
    - `packages/api-gateway/package.json` contains `"test": "vitest run"` in scripts
    - `packages/api-gateway/vitest.config.ts` exists with `defineConfig` export
    - `packages/ws-gateway/package.json` contains `"vitest": "^3.2.4"` in devDependencies
    - `packages/ws-gateway/package.json` contains `"test": "vitest run"` in scripts
    - `packages/ws-gateway/vitest.config.ts` exists with `defineConfig` export
    - `npx vitest run` exits 0 in both packages
  </acceptance_criteria>
  <done>Vitest configured and runnable in both api-gateway and ws-gateway packages</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix BUG-01, BUG-02, BUG-03, BUG-05 in api-gateway with tests</name>
  <files>
    packages/api-gateway/src/middleware/quota.ts
    packages/api-gateway/src/routes/queries.ts
    packages/api-gateway/src/routes/auth.ts
    packages/api-gateway/src/middleware/__tests__/quota.test.ts
    packages/api-gateway/src/routes/__tests__/queries.test.ts
    packages/api-gateway/src/routes/__tests__/auth.test.ts
  </files>
  <read_first>
    packages/api-gateway/src/middleware/quota.ts (MUST read current catch block at lines 68-71)
    packages/api-gateway/src/routes/queries.ts (MUST read line 76 for error storage, line 104 for header)
    packages/api-gateway/src/routes/auth.ts (MUST read lines 268-308 for session flow, line 418 area for JSON.parse)
    packages/api-gateway/src/types.ts (understand Env bindings type)
    packages/api-gateway/src/services/org-db.ts (understand OrgDatabase interface for mocking)
  </read_first>
  <behavior>
    BUG-01 (quota.test.ts):
    - Test: When quota DB query throws, middleware returns 503 with `{ success: false, error: { code: 'SERVICE_UNAVAILABLE' } }`
    - Test: When quota DB query throws, middleware does NOT call next()

    BUG-02 (queries.test.ts):
    - Test: When AI orchestrator returns error, the error_message stored in DB is exactly 'AI generation failed' (no raw error body appended)
    - Test: Raw error body is logged via console.error (not swallowed)

    BUG-03 (auth.test.ts):
    - Test: During token refresh, KV put() for new session is called BEFORE KV delete() for old session
    - Test: Order verification -- capture call order of CACHE.put and CACHE.delete

    BUG-05 (auth.test.ts):
    - Test: When KV returns malformed JSON for reset token, endpoint returns 401 status
    - Test: When KV returns malformed JSON, response body contains error code 'UNAUTHORIZED'
  </behavior>
  <action>
**Write tests first (RED), then fix bugs (GREEN).**

**BUG-01 Fix** in `packages/api-gateway/src/middleware/quota.ts` lines 68-71:
Replace the catch block. Change from:
```typescript
catch (err) {
  console.error('Quota check error:', err);
  return next();
}
```
To:
```typescript
catch (err) {
  console.error('Quota check error:', err);
  return c.json({
    success: false,
    error: {
      code: 'SERVICE_UNAVAILABLE',
      message: 'Unable to verify quota. Please try again later.',
    },
  }, 503);
}
```

**BUG-02 Fix** in `packages/api-gateway/src/routes/queries.ts` around line 76:
Change from:
```typescript
await orgDb.updateQuery(queryId, { status: 'error', error_message: `AI generation failed: ${errorBody}` });
```
To:
```typescript
console.error('AI generation error for query', queryId, ':', errorBody);
await orgDb.updateQuery(queryId, { status: 'error', error_message: 'AI generation failed' });
```

**BUG-03 Fix** in `packages/api-gateway/src/routes/auth.ts`:
Move the `c.env.CACHE.delete(sessionKey)` call (currently at line ~269, before token creation) to AFTER the `Promise.all([c.env.CACHE.put(...), c.env.CACHE.put(...)])` calls at lines ~305-307. The new order must be:
1. Create new JWT tokens
2. `await Promise.all([c.env.CACHE.put(session:${jti}, ...), c.env.CACHE.put(session:${refreshJti}, ...)])`
3. `await c.env.CACHE.delete(sessionKey)` -- delete old AFTER new is stored

**BUG-05 Fix** in `packages/api-gateway/src/routes/auth.ts` around line 418:
Wrap the `JSON.parse(resetData)` call in try-catch:
```typescript
let parsed: { userId: string; orgId: string };
try {
  parsed = JSON.parse(resetData);
} catch {
  return c.json({ success: false, error: { code: 'UNAUTHORIZED', message: 'Invalid or expired reset token' } }, 401);
}
const { userId, orgId } = parsed;
```

For tests, mock the Hono context (`c`) with mock env bindings:
- Mock `c.env.DB` with a `prepare().bind().first()` chain that throws for BUG-01 test
- Mock `c.env.CACHE` with spied `put`/`delete` methods for BUG-03 ordering test
- Mock `orgDb.updateQuery` to capture the error_message argument for BUG-02 test
  </action>
  <verify>
    <automated>cd /Users/address0/Documents/Repos/apura/packages/api-gateway && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `quota.ts` catch block contains `return c.json(` and `503` (no `return next()` in catch)
    - `queries.ts` error storage line contains exactly `error_message: 'AI generation failed'` with no template literal interpolation
    - `queries.ts` has `console.error('AI generation error` before the updateQuery call
    - `auth.ts` has `CACHE.delete` appearing AFTER `CACHE.put` in the refresh token flow (not before)
    - `auth.ts` has `try { parsed = JSON.parse(resetData)` pattern near the reset password endpoint
    - `auth.ts` has `catch` block returning 401 for JSON parse failure
    - All test files exist: `quota.test.ts`, `queries.test.ts`, `auth.test.ts`
    - `npx vitest run` passes with 0 failures in api-gateway
  </acceptance_criteria>
  <done>BUG-01, BUG-02, BUG-03, BUG-05 fixed with passing tests proving each fix</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix BUG-04 in ws-gateway -- timing-safe secret comparison</name>
  <files>
    packages/ws-gateway/src/index.ts
    packages/ws-gateway/src/__tests__/internal-secret.test.ts
  </files>
  <read_first>
    packages/ws-gateway/src/index.ts (MUST read lines 45-49 for current secret check)
    packages/ws-gateway/src/types.ts (understand Env type)
  </read_first>
  <behavior>
    - Test: When header value matches INTERNAL_SECRET, request is allowed (not 401)
    - Test: When header value does NOT match, request returns 401
    - Test: When header is missing, request returns 401
    - Test: The comparison uses crypto.subtle.timingSafeEqual (not string ===)
    - Test: When header and secret have different lengths, returns 401 (no crash)
  </behavior>
  <action>
**Write tests first (RED), then fix (GREEN).**

1. Create a `timingSafeCompare` helper function in `packages/ws-gateway/src/index.ts` (or a separate utils file):
```typescript
function timingSafeCompare(a: string, b: string): boolean {
  const encoder = new TextEncoder();
  const bufA = encoder.encode(a);
  const bufB = encoder.encode(b);
  if (bufA.byteLength !== bufB.byteLength) return false;
  return crypto.subtle.timingSafeEqual(bufA, bufB);
}
```

2. Replace line 47 in `packages/ws-gateway/src/index.ts`:
Change from:
```typescript
if (internalSecret !== env.INTERNAL_SECRET) {
```
To:
```typescript
if (!internalSecret || !timingSafeCompare(internalSecret, env.INTERNAL_SECRET)) {
```

3. In the test file, since `crypto.subtle.timingSafeEqual` may not be available in Node test environment, mock it or use a polyfill. The test should verify:
   - The function is called (spy on crypto.subtle.timingSafeEqual)
   - Correct behavior for matching/non-matching/missing header values

Note: Since `crypto.subtle.timingSafeEqual` is a Workers runtime API available via `nodejs_compat`, it should also be available in Node.js 20+ test environment. If not, mock `crypto.subtle` in the test.
  </action>
  <verify>
    <automated>cd /Users/address0/Documents/Repos/apura/packages/ws-gateway && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `ws-gateway/src/index.ts` contains `timingSafeCompare` or `timingSafeEqual` function definition
    - `ws-gateway/src/index.ts` line with secret check uses `timingSafeCompare` (NOT `!==`)
    - `ws-gateway/src/index.ts` does NOT contain `internalSecret !== env.INTERNAL_SECRET`
    - Test file `ws-gateway/src/__tests__/internal-secret.test.ts` exists
    - `npx vitest run` passes with 0 failures in ws-gateway
  </acceptance_criteria>
  <done>BUG-04 fixed -- internal secret validated with timing-safe comparison, tests passing</done>
</task>

</tasks>

<verification>
All 5 bugs fixed and verified:
- `cd packages/api-gateway && npx vitest run` -- all tests pass
- `cd packages/ws-gateway && npx vitest run` -- all tests pass
- `npm run test` at root runs both packages via Turbo
- `npm run typecheck` passes across all packages
</verification>

<success_criteria>
- 5 bug fixes applied across 4 source files
- Vitest infrastructure in api-gateway and ws-gateway
- Test files proving each bug fix behavior
- All tests green, typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-bug-fixes-and-cicd/01-01-SUMMARY.md`
</output>
